# GENERATED FILE PLACEHOLDER.
# Generate this from the live installer, or regenerate it on the installed
# Disko system with nixos-generate-config --show-hardware-config --no-filesystems.
# nixos-generate-config will replace this entire file.
{ ... }:
{
  assertions = [
    {
      assertion = false;
      message = "arch-desktop hardware configuration has not been generated. From the live installer run ./nixos/scripts/prepare-arch-desktop.sh /mnt. On an already-installed Disko system, replace this file with: sudo nixos-generate-config --show-hardware-config --no-filesystems.";
    }
  ];
}

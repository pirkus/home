# GENERATED FILE PLACEHOLDER.
# Run ./nixos/scripts/prepare-arch-desktop.sh /mnt from the NixOS installer.
# nixos-generate-config will replace this entire file.
{ ... }:
{
  assertions = [
    {
      assertion = false;
      message = "arch-desktop hardware configuration has not been generated. Mount the target root at /mnt and run ./nixos/scripts/prepare-arch-desktop.sh /mnt first.";
    }
  ];
}
